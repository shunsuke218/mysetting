public class Entry {

    String first;
    String last;

    public Entry(String first, String last) {
        this.first = first;
        this.last = last;
    }

   
    public String toString() {
        //return "Entry{" + "first=" + first + ", last=" + last + '}';
        return first + " " + last;
    }

}
